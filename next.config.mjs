/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false, // Disable React Strict Mode         images: {
    images: {
        domains: ["lh3.googleusercontent.com"], // Add the hostname here
    }
    // ... other configurations ...
};

export default nextConfig;
